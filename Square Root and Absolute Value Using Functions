#include <stdio.h>
#include <math.h>

int main() {
    double num;
    printf("Enter a number: ");
    scanf("%lf", &num);

    printf("Square Root = %.2lf\n", sqrt(num));
    printf("Absolute Value = %.2lf\n", fabs(num));
    return 0;
}
